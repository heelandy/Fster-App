==================================================================
FOSTER CARE HOME MANAGEMENT SYSTEM — SPECIFICATION & BUILD STATUS
==================================================================
A secure, private, mobile-friendly web app for foster parents to manage
placements, appointments, documents, routines, expenses, contacts,
medications and licensing — plus a full platform-admin console.

Build verified: Prisma schema valid · Next.js production build passing ·
TypeScript typecheck passing · 74 unit tests passing · cybersecurity audit
PASS (SECURITY_AUDIT_REPORT.md). See README.md / DEPLOYMENT.md.

Milestones:  Core app .......... 2026-06-15
             Admin platform .... 2026-06-16
             Finance + admin user-management batch .... 2026-06-17
             Agency portal + case-worker roles .... 2026-06-19
             Caregiver suite + agency workflow (goals, scheduled visits,
               messaging, reports, behaviour/closet/journal/timeline, PDFs) .... 2026-06-20
             Records cluster (court / education / immunizations / training) +
               signup role picker + agency email staff invites +
               foster-parent visit logging .... 2026-06-22
             Consent-based agency oversight (in-app approve/deny + revoke) +
               super-admin plan editing + Stripe price sync +
               per-request query caching .... 2026-06-23

LEGEND:  ✅ done   🟡 partial   ➖ not applicable to this product
         ⬜ not built (future / larger-scope vision)

This document is organized in seven parts, with every requirement listed
once and marked with its status:

  PART 1 — Core product (the foster-parent app)
  PART 2 — Admin platform (the 25-section admin spec)
  PART 3 — Admin capabilities, restrictions & role tiers (consolidated)
  PART 4 — Future / out-of-scope vision (incident workflow + AI)
  PART 5 — Security threat model & defense status
  PART 6 — Billing capabilities & status
  PART 7 — Agency & staff roles (multi-tenant agency portal)
  SUMMARY


==================================================================
PART 1 — CORE PRODUCT (FOSTER-PARENT APP)
==================================================================
Goal: a private app for foster parents to manage placement information,
appointments, documents, routines, expenses, contacts and licensing —
simple, secure, mobile-friendly, for real daily use.

------------------------------------------------------------------
CORE FEATURES
------------------------------------------------------------------
1. Child Profile Management ✅
   Name / preferred name, age, date of birth, placement date, case number,
   caseworker, school, doctor, allergies, medications, important notes,
   emergency contacts, placement status. Sensitive fields are access-controlled.

2. Appointments & Calendar ✅
   Doctor, therapy, dental, court dates, school meetings, caseworker visits,
   home inspections, licensing deadlines. Reminders + calendar views.

3. Document Storage ✅
   Secure upload/storage of placement paperwork, medical/school/court/
   licensing documents, receipts, case notes. Private + access-controlled.

4. Daily Care Logs ✅
   Meals, sleep, behavior, mood, school updates, visits, medical concerns,
   incidents, milestones. Date-based history.

5. Medication Tracking ✅
   Name, dosage, schedule, start/end date, prescribing doctor, notes,
   missed doses. Includes reminders and a give/miss/refuse log.

6. Expense Tracking ✅
   Clothing, school supplies, food, hygiene, activities, transportation,
   medical, childcare, room setup, receipts. Monthly summaries + CSV export.

7. Contact Management ✅
   Caseworker, licensing worker, GAL, attorney, therapist, doctor, dentist,
   teacher, school counselor, biological family, emergency contacts —
   with phone, email, agency, notes and role.

8. Routines & Checklists ✅
   Reusable morning / after-school / bedtime / visit-day / school-day /
   intake / home-inspection / medication checklists. Mark tasks complete.

9. Licensing & Compliance Tracker ✅
   Training hours, fingerprinting, background checks, home-study items,
   fire inspection, medical forms, pet vaccinations, CPR/First Aid,
   annual renewal deadlines, required documents. Shows upcoming deadlines.

------------------------------------------------------------------
USER ROLES ✅
------------------------------------------------------------------
• Foster Parent — manage household, create/edit child profiles, upload
  documents, track appointments/expenses/medications/notes.
• Co-Parent / Household Member — view and help manage approved items;
  permissions can be limited.
• Babysitter / Respite Caregiver — limited care instructions only; no
  private case documents, no court/legal information.
• Admin — manage users and system settings; CANNOT casually view private
  child data unless explicitly granted.

------------------------------------------------------------------
PAYMENTS & SUBSCRIPTIONS (Stripe) ✅
------------------------------------------------------------------
Subscription plans ✅
• Free — one household, one child profile, limited checklists, limited
  appointment tracking, no document upload/exports.
• Family — multiple child profiles, appointments, daily care logs,
  medication & expense tracking, basic document uploads.
• Pro Foster Parent — unlimited child profiles, full document storage,
  licensing tracker, export reports, advanced reminders, co-parent access,
  babysitter limited-access mode.
• Agency / Multi-Home — multiple homes, agency dashboard, household-level
  permissions, compliance overview, exportable reports.

Payment plan options ✅
Monthly billing · annual billing · promo codes · trial (if enabled) ·
upgrade · downgrade · cancel · resume · update payment method · view
invoices · download receipts.

Stripe requirements ✅
Checkout · Customer Portal · Webhooks · subscription-status syncing ·
failed-payment handling · grace period · automatic access removal &
restoration · signature-verified webhooks · coupon/promo support.
Raw card information is never stored (entry happens on Stripe-hosted pages).

------------------------------------------------------------------
SECURITY & PRIVACY ✅
------------------------------------------------------------------
Secure authentication · role-based access control · household-based
permissions · limited-caregiver mode · password hashing (bcrypt 12) ·
input validation (Zod) · secure file storage · private document URLs ·
authenticated file access · API rate limiting · audit logs + admin audit
logs · secure session handling · protection against SQLi / XSS / CSRF /
IDOR / brute-force · secrets in environment variables · error handling that
never leaks private data.
Invariant: child information, case numbers, documents and private notes are
never exposed to unauthorized users.

------------------------------------------------------------------
CYBERSECURITY TEST ✅  (SECURITY_AUDIT_REPORT.md — verdict PASS)
------------------------------------------------------------------
Reviewed: broken auth, broken access control, IDOR, exposed child data,
exposed documents, insecure upload, public-bucket mistakes, SQLi, XSS, CSRF,
weak password handling, admin-route exposure, webhook spoofing, rate-limit
bypass, sensitive-data leakage, dependency vulnerabilities.
Performed: static review · dependency scan · route-permission audit · file-
storage audit · auth-flow audit · payment-flow audit · OWASP Top 10 checklist.
All critical & high-risk findings fixed before sign-off.

------------------------------------------------------------------
DATABASE MODELS ✅  (prisma/schema.prisma)
------------------------------------------------------------------
User, Household, HouseholdMember, ChildProfile, Placement, Appointment,
Medication, MedicationLog, DailyCareLog, Expense, Receipt, Document, Contact,
Routine, RoutineTask, Checklist, ChecklistItem, LicensingRequirement,
TrainingHour, Subscription, Payment, Invoice, Plan, AdminAuditLog,
SecurityAuditLog — plus PasswordResetToken, EmailVerificationToken,
SupportTicket, SupportMessage, HouseholdInvite, Setting, Notification.

------------------------------------------------------------------
TECH STACK ✅
------------------------------------------------------------------
Next.js (App Router) · TypeScript · PostgreSQL · Prisma · Tailwind CSS ·
NextAuth (credentials) · Stripe · Zod · private file storage · rate-limit
middleware · AES-256-GCM field/file encryption · Vitest.

------------------------------------------------------------------
DELIVERABLES ✅
------------------------------------------------------------------
Project structure · DB schema · auth system · role-based permissions ·
foster-care dashboard · child-profile system · appointment calendar ·
document upload · daily care logs · medication tracker · expense tracker ·
contact manager · routine/checklist system · licensing tracker · Stripe
payments · subscriber dashboard · admin dashboard · security protections ·
cybersecurity audit report · setup instructions · .env.example · tests.

------------------------------------------------------------------
ACCEPTANCE CRITERIA ✅  (all met)
------------------------------------------------------------------
Create household ✔ · create child profiles ✔ · track appointments ✔ ·
upload private documents securely ✔ · log medications & daily care ✔ ·
track expenses & receipts ✔ · manage contacts ✔ · create routines &
checklists ✔ · track licensing ✔ · subscribe to paid plans ✔ · Stripe
billing works ✔ · free users gated from paid features ✔ · babysitters see
only limited care info ✔ · unauthorized users cannot view child data ✔ ·
admin routes protected ✔ · cybersecurity audit complete ✔ ·
SECURITY_AUDIT_REPORT.md exists ✔ · critical/high issues fixed ✔ ·
app runs locally with no errors ✔.


==================================================================
PART 2 — ADMIN PLATFORM (25-section spec)
==================================================================
The admin console lives at /admin in its own route group, gated by an ADMIN
JWT + a granular permission matrix (lib/admin.ts). 11 tabs: Overview · Users ·
Tickets · Analytics · Finance · Notifications · Settings · Integrations ·
System · Security log · Admin log.

 1. Admin purpose (control center) ✅
    One dashboard to manage the whole app without direct database access.

 2. Admin user roles (7 tiers) ✅
    SUPER_ADMIN · ADMIN · MANAGER · SUPPORT · MODERATOR · FINANCE_ADMIN ·
    READ_ONLY. (Consolidated capability/restriction table is in PART 3.)

 3. Admin login system ✅ 🟡
    ✅ email+password, 2FA (TOTP) with scannable QR, password reset,
       login-attempt limits, IP logging, session timeout, forced logout,
       admin activity logging, AND a per-device active-session list with
       selective revoke (/account → Active sessions).
    🟡 no separate admin-only login page (admins use the same hardened
       /login; protection is 2FA + RBAC, not a distinct URL).

 4. Admin dashboard ✅
    Tiles: total/active/new users, suspended, revenue, failed payments,
    open tickets, security alerts (24h), server/database/API status,
    recent admin actions. ("Understand the app in <30s.")

 5. User management module ✅
    Search/filter; view account metadata; create user (emails set-password
    link); edit name/email; reset password (admin triggers the link, never
    sees the password); verify account; suspend; ban; reactivate; unlock;
    add internal note; assign role; soft-delete (deactivate + tombstone,
    restorable). All actions audited old→new. Never exposes hashes/child data.

 6. Admin management (manage admins) ✅
    Create admin (create-user + adminRole, needs admins.manage) · set/change
    admin role · disable (suspend) · force password reset · limit permissions
    (role matrix) · view admin activity (admin audit log) · remove admin access.

 7. Permission system (granular) ✅
    adminCan() matrix enforced per route: users.view/edit/suspend/delete/note,
    admins.manage, payments.view/refund, settings.update, content.moderate,
    support.manage, analytics.view, system.view, logs.view, reports.export.

 8. Content management ➖
    Not applicable — a private foster-care app has no public/user-generated
    content (posts, products, lessons) to moderate.

 9. Reports & moderation ➖
    Not applicable — no public UGC or user-on-user reporting. (User safety is
    handled via Support tickets, §11, and account actions, §5.)

10. Payments & subscription module ✅
    Finance tab: recent payments, subscriptions by tier/status, revenue,
    payment issues. In-app Stripe refunds (full/partial) and account credits.
    Mark payment issue (via status) · export financial report (CSV).
    🟡 plan catalogue is code+Stripe-config (no plan-CRUD UI); coupons via Stripe.

11. Support ticket module ✅
    User threads (/support) + staff Tickets tab: reply, set status
    (OPEN/PENDING/RESOLVED/CLOSED), priority. Admin notified on new ticket.

12. Settings module ✅
    System settings: app name, maintenance mode, signups enabled, email-
    verification required. Integrations tab (SUPER_ADMIN + TOTP step-up):
    live Stripe keys/prices + email config, no code/env edits.

13. Analytics & reporting ✅
    DAU/WAU/MAU, stickiness, signups & active-user charts (inline SVG), churn,
    revenue, KPIs. CSV report export (users/subscriptions/revenue).

14. Audit log system ✅
    AdminAuditLog + SecurityAuditLog: actor, action, target, old/new value,
    IP, timestamp. Append-only — immutable (no edit/delete route exists).

15. Notification system ✅
    Internal admin alerts on new signup, failed payment, new support ticket;
    unread counter in the console.

16. Security architecture ✅ 🟡
    HTTPS/HSTS, 2FA, strong passwords, session expiry, role permissions, IP
    logging, rate limiting, CSRF, XSS (nonce CSP), SQLi-safe (Prisma),
    AES-256-GCM encryption at rest, admin action logs, no direct DB editing.
    🟡 backup system is operational (see §22), not application code.

17. Database structure ✅
    users/admin-roles, support_tickets, settings, notifications, invites,
    reset/verification tokens, payments, subscriptions, audit_logs — all present.

18. Backend architecture (services) ✅
    Clean lib/ layer: auth, authz, admin, totp, tokens, email, stripe,
    billing-sync, notify, settings, audit, rate-limit, storage, config, etc.

19. Frontend admin panel ✅
    11 pages/tabs (see header). One web interface controls the app.

20. API structure (admin routes) ✅
    Every /api/admin/* route is auth + permission gated: users (GET/POST),
    users/[id] (PATCH/DELETE), payments, payments/[id]/refund, payments/credit,
    reports/export, tickets, analytics, health, settings, notifications, audit,
    step-up, integrations.

21. System health module ✅
    DB latency, storage usage, memory, uptime, CPU% + cores + load avg,
    integration status, and config-validation warnings (incl. placeholder/
    invalid Stripe & email keys). Process-level metrics (no external APM).

22. Backup & recovery 🟡
    Operational, documented in DEPLOYMENT.md (managed Postgres backups /
    pg_dump cron, encrypted storage, restore testing) — not application code.

23. Admin workflow ✅
    Login (2FA) → check dashboard alerts → review new users → answer tickets →
    check failed payments → review system/security → export reports → review
    audit logs → log out. Supported by the tabs above.

24. Recommended tech stack ✅
    Documented variant chosen: Next.js route handlers (not NestJS), NextAuth
    (not a separate JWT service), Resend email, local private storage (S3/R2
    pluggable), optional Redis rate limit, optional error-reporting webhook.

25. Final architecture summary ✅
    ~22/25 areas fully implemented; §8/§9 not applicable; §22 operational.


==================================================================
PART 3 — ADMIN CAPABILITIES, RESTRICTIONS & ROLE TIERS
==================================================================
(Consolidates the former "What an Admin CAN/SHOULD NOT Do" and the three
separate role lists into one place.)

------------------------------------------------------------------
WHAT AN ADMIN CAN DO  — all implemented
------------------------------------------------------------------
User management ✅ — view accounts · create users · edit info · suspend ·
  ban · reset passwords (trigger link) · unlock · verify accounts · assign roles.
Support operations ✅ — view tickets · respond · escalate (priority) · close.
Business operations ✅ — view subscriptions/invoices/payments · issue refunds ·
  apply credits · 🟡 manage plans (code/Stripe config).
Security operations ✅ — view login history (security log) · view suspicious
  activity · force-logout sessions · lock compromised accounts (suspend/ban).
Reporting & analytics ✅ — view analytics · export reports (CSV) · monitor
  KPIs · track revenue.
System monitoring ✅ — view system health/server status/logs · monitor
  performance · monitor errors (optional error-webhook).
Content management ➖ — N/A (no UGC; see §8/§9).

------------------------------------------------------------------
WHAT AN ADMIN MUST NOT DO  — all enforced
------------------------------------------------------------------
✅ Never see passwords/PINs/secret questions/auth tokens — only bcrypt hashes
   exist; admin can only trigger a password reset.
✅ Never read private child/medical/legal data — requireHousehold has no admin
   bypass; admin views are aggregate counts only; data encrypted at rest.
✅ Audit logs immutable — append-only; no edit/delete/hide path.
✅ Cannot bypass security controls — no toggle disables logging/monitoring.
✅ No direct production-DB access — everything goes through guarded routes.
✅ Payment-provider secrets restricted — Stripe keys encrypted, masked,
   SUPER_ADMIN + TOTP step-up only; encryption keys are env-only.
✅ Financial info masked / no raw card data — card entry is Stripe-hosted.
✅ No permanent hard-delete of records — soft delete / deactivate / restore.

------------------------------------------------------------------
ADMIN ROLE TIERS ✅  (7 implemented; capabilities via the permission matrix)
------------------------------------------------------------------
SUPER_ADMIN  — everything: create admins, change permissions, configure system,
               integrations. Cannot see passwords, delete audit logs, or
               decrypt protected data without authorization.
ADMIN        — manage users, content moderation, daily operations; not critical
               system settings unless granted.
MANAGER      — view reports, supervise; limited access to sensitive data.
SUPPORT      — view users, edit basic info, reset passwords, handle tickets;
               no finance / security / infrastructure.
MODERATOR    — handle reports & user safety actions; no payments / settings / DB.
FINANCE_ADMIN— view payments, process refunds, generate financial reports;
               cannot modify users or infrastructure.
READ_ONLY    — view dashboards/reports only; no changes.
(➖ "System/Infrastructure Admin" is an operational ops role, not an in-app tier.)

------------------------------------------------------------------
AUDIT-LOG REQUIREMENT ✅  (single source of truth)
------------------------------------------------------------------
Every important action is recorded — login, account changes, role/permission
changes, subscription/payment changes (refund, credit), settings changes,
suspension/deletion. Each entry stores actor, action, target, old value, new
value, date/time, IP (and device where available). Nothing is editable after
it is logged.


==================================================================
PART 4 — FUTURE / OUT-OF-SCOPE VISION (multi-agency platform)
==================================================================
The notes below describe a LARGER product than the shipped private foster-
parent app: a multi-tenant agency platform with case-worker accounts, agency
administration and AI. These are tracked as future scope, not built.

NOTE: a working multi-tenant agency portal now ships — see PART 7. The items
below now read against PART 7's status (🟡 = partly shipped, ⬜ = still future).

🟡 Agency as a tenant — managing multiple foster-care organizations.
   (BUILT in PART 7: agencies, staff roles, home oversight, per-agency data isolation.)
🟡 CASE WORKER role — maintain case records, end placements, monitor compliance.
   (BUILT in PART 7: create homes, assign children, trial→active→reunified
   placements, submit licensing. STILL FUTURE: cross-family search/match,
   transfer between homes, contact biological family.)
🟡 AGENCY ADMIN role — oversee placements, manage staff & homes. (BUILT in PART 7:
   staff + home management and the oversight dashboard. STILL FUTURE: approve/
   reject foster-parent applications, override placement decisions, process
   reimbursements/stipends, generate state/federal/outcome reports, workflow templates.)
🟡 Placement lifecycle — trial → active (placed until reunified) → reunified/ended
   now ships (PART 7). STILL FUTURE: placement notifications, accept/decline
   requests, placement matching, transfer between homes, full history timeline.
⬜ Incident management workflow — foster parent reports → case worker
   investigates → agency escalates/closes/reports to state. (Today incidents
   are free-text in daily care logs; no formal workflow.)
⬜ AI features — placement-matching recommendations, missing-document alerts,
   compliance monitoring, risk scoring, report generation, onboarding assistant.
⬜ Reimbursement/stipend financials & tax forms for foster parents.

✅ GOLDEN RULE (privacy principle) — already enforced today:
   A foster parent sees only what's needed to care for their child; no one,
   including administrators, can view passwords, delete audit history, or
   bypass legal/privacy protections. Foster-care data is highly sensitive
   child-welfare information and is protected accordingly.


==================================================================
PART 5 — SECURITY THREAT MODEL & DEFENSE STATUS
==================================================================
(Sorted from the appended attack taxonomy. Each threat is marked with the
app's current defense: ✅ defended in app code · 🟡 partial / needs an option
enabled · ⬜ operational / infrastructure (not app code) · ➖ not applicable.)
Verified against: middleware CSP, next.config security headers, lib/authz,
lib/auth, lib/request (CSRF), lib/storage, lib/crypto, Stripe webhook handling.

------------------------------------------------------------------
PRIORITY DEFENSES FOR A FOSTER-CARE APP (highest first)
------------------------------------------------------------------
 1. Broken access control ........ ✅ household-scoped RBAC, no admin bypass
 2. IDOR ......................... ✅ every query joins householdId; 404 cross-tenant
 3. SQL injection ................ ✅ Prisma parameterized; no raw SQL
 4. XSS .......................... ✅ React escaping + nonce CSP + email escapeHtml
 5. CSRF ......................... ✅ Origin/Referer check (no fail-open) on mutations
 6. Account takeover ............. ✅ bcrypt(12), lockout, rate limit, TOTP 2FA, forced logout
 7. API authorization ............ ✅ per-route capability + admin permission matrix
 8. File upload .................. ✅ MIME+magic-byte, size cap, private store, authed download
                                   🟡 no malware/AV scan
 9. Insider threats .............. ✅ admins can't see child data/passwords; immutable audit
10. Cloud misconfiguration ....... ⬜ deploy-time (private buckets/DB, least-priv IAM)
11. Data leaks ................... ✅ generic errors, private storage, secrets in env
12. Ransomware ................... ⬜ operational (backups + server hardening)
13. DDoS ........................ ⬜ operational (CDN/WAF, e.g. Cloudflare)
14. Supply chain ................ 🟡 minimal pinned deps; needs audit + Dependabot
15. Social engineering .......... 🟡 2FA blunts phished passwords; rest is process

------------------------------------------------------------------
FULL THREAT TAXONOMY → DEFENSE
------------------------------------------------------------------
1.  Authentication attacks ✅
    Brute force / spraying / credential stuffing → account lockout + login
    rate limit (in-memory, optional Redis) + failed-login tracking. Session
    hijack/fixation → signed httpOnly JWT, session timeout, tokenVersion
    forced-logout. JWT forgery → signed w/ NEXTAUTH_SECRET; role/tokenVersion
    re-read from DB each request. MFA → TOTP 2FA (QR + backup codes).
    Reset/magic-link abuse → hashed single-use 1h tokens, anti-enumeration.
    (OAuth ➖ — credentials provider only.)

2.  Authorization attacks ✅
    Privilege escalation (H/V), IDOR/BOLA, broken access control, forced
    browsing, API permission bypass → deny-by-default capability matrix,
    household-scoped queries with no admin bypass, requireUser reads role
    fresh, admin permission matrix enforced per route.

3.  Injection attacks ✅
    SQLi → Prisma parameterized. Command/OS → no shell exec from input.
    Email-header/CRLF → email sent via Resend JSON API (no raw SMTP headers).
    NoSQL/LDAP/XPath/XML ➖ — not used (PostgreSQL only).

4.  Cross-site scripting (XSS) ✅
    Stored/reflected/DOM → React auto-escaping, strict nonce CSP (script-src
    'self' 'nonce' 'strict-dynamic'), object-src 'none', base-uri 'self';
    email HTML escaped (escapeHtml).

5.  CSRF ✅
    assertSameOrigin (trusted Origin, else trusted Referer, else 403 — no
    fail-open) on every mutation; SameSite cookies; CSP form-action 'self'.

6.  API attacks ✅ 🟡
    BOLA/function-level → scoped + permission matrix. Mass assignment → Zod
    whitelists fields. Excessive data exposure → explicit select (no hashes /
    child data). 🟡 rate-limit is per-instance unless Upstash Redis is enabled.
    GraphQL ➖ (REST only).

7.  Business-logic attacks ✅ 🟡
    Payment/subscription bypass → entitlements derive ONLY from signed Stripe
    webhooks (idempotent/dedup). Refunds/credits → admin-only + audited.
    Coupons/trials → enforced by Stripe. 🟡 plan-limit checks can race under
    concurrency (low impact).

8.  File-upload attacks ✅ 🟡
    Web shell/executable/polyglot/type-bypass → MIME + magic-byte validation,
    size pre-check, files stored OUTSIDE the web root and served only via an
    authenticated, ownership-checked route with attachment + nosniff.
    🟡 no malware/AV scanning (add ClamAV / a scanning API for untrusted files).

9.  Denial of service (DoS) 🟡 ⬜
    App-layer → rate limiting + bounded body/field sizes. ⬜ true DoS
    resilience (slowloris, floods) belongs to a CDN/WAF + infra.

10. Distributed DoS (DDoS) ⬜
    Operational — front with a CDN/WAF (Cloudflare/Fastly); not app code.

11. Session attacks ✅
    Hijack/fixation/prediction → signed httpOnly SameSite JWT, rotation on
    privilege change, tokenVersion revocation, session timeout, AND a
    per-device session list with selective revoke (UserSession rows; a revoked
    device fails requireUser on its next request). "Sign out everywhere" also
    revokes all rows. (/account → Active sessions.)

12. Browser-based attacks ✅
    Clickjacking → X-Frame-Options: DENY + CSP frame-ancestors 'none'. MIME
    sniffing → X-Content-Type-Options: nosniff. Referrer leakage →
    Referrer-Policy. Powered-by hidden; Permissions-Policy locks camera/mic/geo.

13. DNS attacks ⬜
    Operational — registrar lock, DNSSEC, monitor for sub-domain takeover.

14. Email-based attacks 🟡 ⬜
    App → escaped HTML, rate-limited reset/verification sends, anti-enumeration.
    ⬜ SPF/DKIM/DMARC are DNS records to set for the sending domain.

15. Supply-chain attacks 🟡
    Minimal, version-pinned dependencies (lockfile); the QR encoder added is a
    single zero-dependency package. 🟡 enable Dependabot / `npm audit` in CI and
    review the existing transitive advisories (Next.js tree).

16. Cloud attacks ⬜
    Deploy-time — private object storage (no public buckets), non-public DB,
    least-privilege IAM, no container privilege. App keeps S3 keys private +
    validates storage keys.

17. Database attacks ✅ 🟡
    Enumeration/extraction → parameterized access, least-privilege app role,
    AES-256-GCM field encryption for sensitive child/medical fields + 2FA secret.
    🟡 backup encryption + DB-credential rotation are operational.

18. Cryptographic attacks ✅ 🟡
    AES-256-GCM, bcrypt(12), CSPRNG (crypto.randomBytes), HMAC-signed tokens,
    HSTS (prod). 🟡 ENCRYPTION_KEY must be backed up + rotated off-app; TLS/cert
    management is operational.

19. Malware attacks ⬜
    Operational — server hardening, EDR/AV, offline backups for recovery.
    (Upload AV scanning is the app-side piece — see §8.)

20. Insider threats ✅ 🟡
    Admins cannot read child data or passwords; least privilege; every admin
    action audited to an immutable log; no direct DB editing. 🟡 SUPER_ADMIN is
    powerful (mitigated: no child-data/password/decryption access; TOTP step-up
    for secrets).

21. Social engineering 🟡
    2FA limits damage from phished passwords; support tickets warn against
    sharing PII. Rest is people/process (verification scripts, staff training).

22. Mobile-app attacks ➖
    No native mobile app today (responsive web only). Revisit if one ships.

23. Infrastructure attacks ⬜
    Operational — patching, firewall, least exposed services, config hardening.

24. Bot attacks 🟡
    Rate limiting + signup toggle + email verification cut fake/abusive signups.
    🟡 no CAPTCHA / bot-scoring on register & login (add if abuse appears).

25. Data-exposure attacks ✅ 🟡
    Generic error responses (no stack traces), private storage, no debug output
    in prod, secrets in env, audit metadata carries no child PII. 🟡 backup/log
    storage privacy is operational.

------------------------------------------------------------------
SUGGESTED "LITTLE BY LITTLE" SECURITY WORKLIST (app-code items)
------------------------------------------------------------------
✅ Per-device session list + selective revoke (§11 / Admin §3) — DONE.
✅ Upload malware/AV scanning hook for untrusted files (§8) — DONE (lib/av-scan,
   gated by AV_SCAN_URL; fails closed).
✅ Optional CAPTCHA / bot-scoring on register (§24) — DONE (Cloudflare Turnstile,
   gated by TURNSTILE_* keys; off by default).
✅ Dependabot + `npm audit` CI gate (§15) — DONE (.github/dependabot.yml + CI).
🟡 Enforce distributed (Redis) rate limiting in production (§6) — built + opt-in
   (set UPSTASH_REDIS_REST_* to enable across instances).
(Operational items — SPF/DKIM/DMARC, CDN/WAF for DoS/DDoS, backups, DNSSEC,
cloud/IAM hardening — live in DEPLOYMENT.md, not app code.)




==================================================================
PART 6 — BILLING CAPABILITIES & STATUS
==================================================================
(Sorted from the appended billing master list. Marked with what the app does
today: ✅ implemented · 🟡 partial / Stripe-managed · ⬜ not built (future) ·
➖ not applicable. All money movement is via Stripe — card data never touches
this server.)

------------------------------------------------------------------
"BUILD FIRST" PRIORITY MODULES → STATUS
------------------------------------------------------------------
 1. Plans .............................. ✅ FREE / Family / Pro / Agency (lib/plans.ts)
 2. Subscriptions ...................... ✅ per-household, monthly / annual
 3. Stripe Checkout .................... ✅ Checkout Session (+ Payment Link option)
 4. Webhooks ........................... ✅ signature-verified, idempotent + pull-reconcile fallback
 5. Billing history .................... ✅ invoices list on /billing
 6. Invoice download ................... ✅ hosted invoice + PDF links (Stripe)
 7. Payment-failure recovery ........... ✅ grace period, status sync, admin alert
 8. Subscription up/downgrade .......... ✅ Customer Portal (Stripe proration)
 9. Refund system ...................... ✅ in-app admin refunds (full / partial)
10. Admin billing dashboard ........... ✅ Finance tab (payments, refunds, credits)
11. Audit logs ........................ ✅ refunds / credits / changes audited
12. Revenue analytics ................. 🟡 revenue + churn; no MRR/ARR/LTV yet
13. Tax handling ...................... ⬜ not built (enable Stripe Tax)
14. Coupon system ..................... ✅ promo codes at checkout
15. Customer self-service ............. ✅ Stripe Customer Portal + /billing

------------------------------------------------------------------
FULL BILLING TAXONOMY → STATUS
------------------------------------------------------------------
1.  Free-plan management ✅ 🟡
    ✅ FREE tier with hard limits (children / appointments / checklists) + feature
    gating. 🟡 trials supported by Stripe; ⬜ no multi-account-abuse / conversion
    tracking.
2.  Subscription management ✅
    Monthly / annual, recurring, renewals, upgrade, downgrade, cancel, reactivate
    (Customer Portal). ➖ multi-year / lifetime / pause not offered.
3.  Payment processing ✅ ➖
    ✅ cards (+ Apple/Google Pay automatically via Stripe Checkout), retries,
    verification. ➖ ACH / wire / PayPal not enabled.
4.  Failed-payment handling ✅
    Declines → Stripe retries; GRACE / PAST_DUE / UNPAID statuses, 7-day grace,
    admin notification, access drops after grace.
5.  Plan changes ✅
    Mid-cycle up/downgrade via the Portal (Stripe proration); entitlements update
    via webhook + reconcile.
6.  Proration 🟡
    Handled automatically by Stripe (Portal / Checkout); no custom proration.
7.  Usage-based billing ➖
    Flat tiers; no metered billing.
8.  Metering ➖
    N/A (no usage billing).
9.  Seat-based billing ➖ 🟡
    Agency multi-home is included in one plan; no per-seat charging.
10. Invoicing ✅
    Invoices synced from Stripe (Invoice model) with hosted + PDF links. ➖ custom
    numbering / credit notes are Stripe-managed.
11. Tax management ⬜
    Not built — enable Stripe Tax (sales tax / VAT / GST) before charging where required.
12. Refunds ✅
    In-app admin full / partial refunds via Stripe, audited. 🟡 no multi-step approval.
13. Chargebacks ⬜
    Handled in the Stripe Dashboard today; no in-app surface.
14. Dispute management ⬜
    Stripe Dashboard; not surfaced in-app.
15. Discounts ✅ ➖
    ✅ promo codes / coupons at checkout (Stripe promotion codes). ➖ referral /
    volume / stackable not built.
16. Trial management 🟡
    Stripe trials sync (trialEndsAt); ⬜ no trial-extension / abuse UI.
17. Enterprise billing ⬜
    Custom contracts / POs / net terms — future (state-agency vision, PART 4).
18. Multi-currency ⬜
    Single currency (USD) today.
19. Fraud prevention ✅ 🟡
    ✅ no card data stored; Stripe Radar by default. 🟡 no app-level trial /
    account-farming controls (see PART 5 §24 bot worklist).
20. Account-status management ✅
    ACTIVE / TRIALING / PAST_DUE / GRACE / UNPAID / CANCELED / INCOMPLETE mapped.
21. Feature entitlement ✅
    Plan→feature matrix (lib/plans.ts) + agency-aware effective tier; access
    re-resolved from the DB on every request.
22. Billing notifications ✅ 🟡
    ✅ admin alerted on failed payment. 🟡 customer renewal / trial-ending emails
    rely on Stripe (app itself sends reset / invite / reminder).
23. Billing audit logs ✅
    Refunds, credits and subscription changes recorded in the admin audit log.
24. Revenue reporting 🟡
    Revenue total + churn in the Analytics tab; ⬜ MRR / ARR / LTV / ARPU / NRR not computed.
25. Customer self-service ✅
    Update card, download invoices, change plan, cancel, reactivate, view history —
    via /billing + the Stripe Customer Portal.
26. Data synchronization ✅
    Idempotent, signature-verified webhooks (dedup by event id) PLUS a pull
    reconcile that recovers missed / delayed / duplicate webhooks (resolves the
    customer by stored id or owner email; stamps tier + household). This is the
    post-purchase upgrade path and runs the auto-return countdown.
27. Security ✅
    PCI: card entry is Stripe-hosted (no card data stored). Webhook signature
    verification; secret key encrypted + masked, SUPER_ADMIN + TOTP step-up only.
28. Admin billing tools ✅ 🟡
    ✅ manual credits, refunds, finance dashboard, audit log. 🟡 manual-invoice /
    subscription-override not built.
29. Accounting integration ⬜
    QuickBooks / Xero / ERP / revenue recognition — future.
30. Scaling challenges ⬜
    High volume, global tax, multi-region — operational / infra (future).

------------------------------------------------------------------
FOSTER-CARE-SPECIFIC BILLING
------------------------------------------------------------------
✅ Individual foster-parent, Family, and Agency plans · parent-managed billing.
🟡 Agency-managed billing — one plan covers the agency's homes (no per-seat charge).
⬜ State-agency contracts, per-child / per-user pricing, staff-seat billing,
   document / SMS / background-check / training / licensing fees, grant /
   scholarship / government-funded accounts, organization invoicing, custom-
   contract & multi-location billing — all future (align with PART 4).

------------------------------------------------------------------
SUGGESTED "LITTLE BY LITTLE" BILLING WORKLIST (app-code)
------------------------------------------------------------------
✅ Tax — Stripe Tax wired into checkout, gated by STRIPE_TAX_ENABLED (§11).
✅ Richer revenue metrics — MRR / ARR / ARPU in the Analytics tab (§24).
✅ Customer billing emails — owner emailed on a plan change (up/down/cancel) (§22).
✅ Admin manual plan grant / comp (grant / scholarship / government-funded accounts)
   — Finance tab; comped plans are skipped by the Stripe reconcile (§28).
✅ Prefer Checkout Sessions over Payment Links to avoid duplicate Stripe customers
   (reconcile now handles both, but Checkout is the clean path).
(Future / Stripe-Dashboard: chargebacks/disputes, enterprise contracts,
multi-currency, accounting integration, usage/seat billing.)
(Chargebacks, enterprise contracts, multi-currency, accounting integration →
future / Stripe-Dashboard for now.)


==================================================================
SUMMARY
==================================================================
The shipped product — a private, secure foster-parent app, a multi-tenant agency
portal, an 11-tab platform-admin console and full billing — is COMPLETE and
verified (build · typecheck · 66 tests · security audit PASS).

✅ DONE
• Core features 1–9 (profiles, appointments, documents, care logs,
  medications, expenses, contacts, routines, licensing).
• User roles (Foster Parent / Co-Parent / Babysitter / Admin).
• Billing (PART 6): Stripe Checkout + Payment Links, signature-verified webhooks
  PLUS a pull-reconcile fallback, invoices, in-app refunds + credits, promo
  codes, Customer Portal, and an automatic post-purchase return (5s countdown).
• Two-factor auth (TOTP) with a scannable QR code (generated locally) + backup codes.
• Security & privacy + cybersecurity audit (PASS); threat model in PART 5.
• Per-device active-session list with selective revoke (§3, threat §11).
• Agency portal (PART 7): multi-tenant agency login, staff roles (admin / case-
  worker / viewer), create / link / oversee homes, assign children with a
  trial→active→reunified placement lifecycle, submit licensing, agency-aware tier.
• Revenue analytics: MRR / ARR / ARPU + DAU/WAU/MAU + churn (Analytics tab).
• Admin billing tools: in-app refunds + credits, CSV export, AND manual plan
  grant / comp for grant / scholarship / government-funded accounts (Finance tab).
• Customer billing emails on plan change; Stripe Tax (gated); System-tab config
  check now flags placeholder/invalid Stripe & email keys + CPU/load metrics.
• Security extras: upload AV-scan hook + CAPTCHA (both gated, off by default) +
  Dependabot/CI supply-chain checks.
• Admin platform: 23 of 25 spec sections implemented — full admin CAN-do list
  (create/edit/verify/reset/force-logout users, soft-delete+restore, in-app
  refunds + credits + plan grants, CSV export) and every MUST-NOT restriction enforced.

🟡 PARTIAL (an option to enable, or ops)
• Separate admin-only login page (admins use hardened /login; 2FA+RBAC) (§3).
• Distributed (Redis) rate limiting — built, opt-in via UPSTASH_REDIS_REST_* (threat §6).
• Stripe Tax / AV-scan / CAPTCHA — built but OFF by default (need keys/activation).
• Backup & recovery — operational runbook, not app code (§22).

➖ NOT APPLICABLE
• Content management (§8) / Reports-moderation (§9) — no public UGC.
• Usage / metered / seat billing (PART 6 §7–9) — flat foster-care plans.
• Native mobile-app threats — responsive web only.

⬜ FUTURE / OUT OF SCOPE
• PART 4 — remaining agency vision: foster-parent application approval, placement
  transfer / matching, formal incident workflow, AI features. (The agency portal,
  case-worker roles & the trial→reunified placement lifecycle now ship — PART 7.)
• Billing: chargebacks/disputes, enterprise contracts, multi-currency,
  accounting integration (handled in the Stripe Dashboard today).
• Operational/infra security (PART 5: DDoS/CDN, DNS, cloud/IAM, malware/EDR).

Deferred by choice: Next.js 14→16 major upgrade (staying on patched 14.2.35).
Pre-launch owner items in DEPLOYMENT.md (managed Postgres + backups, TLS, cron,
live Stripe/Resend/Redis/monitoring, counsel review of /privacy + /terms,
remove demo accounts after the UI check).
==================================================================



==================================================================
PART 7 — AGENCY & STAFF ROLES (multi-tenant agency portal)
==================================================================
The agency platform first sketched as "future" in PART 4 now ships as a working
multi-tenant portal (sign in at /agency-login). An agency oversees its own foster
homes; case workers create homes and run placements; staff are scoped by role.
Each agency sees ONLY its own data — every query is scoped by Household.agencyId.
LEGEND: ✅ built · 🟡 partial · ⬜ future / larger-scope (also tracked in PART 4).

------------------------------------------------------------------
CONCEPT
------------------------------------------------------------------
Agency = the organization that oversees foster homes, children & compliance.
Staff  = the people who work under the agency, each scoped by their role.
Home   = a foster parent's household; an agency oversees the homes linked to it.

------------------------------------------------------------------
ROLES & CAPABILITIES (highest authority → lowest)
------------------------------------------------------------------
Three agency roles ship today (AgencyRole enum): AGENCY_ADMIN, CASE_WORKER,
AGENCY_VIEWER. The finer staff titles further below (Social Worker, Licensing
Specialist, Intake Coordinator, Supervisor, Admin Assistant) are future
sub-roles — listed once here, not yet distinct in code.

AGENCY ADMIN  (agency director / owner of the tenant)
  ✅ Manage agency profile & settings
  ✅ Add / remove staff and set each one's role
  ✅ Create a new foster home for a foster parent
  ✅ Link an existing home by the foster parent's email
  ✅ Unlink a home (oversight ends; the foster parent keeps it)
  ✅ View every linked home's oversight detail
  ✅ Assign children, manage placement status, submit licensing
  ✅ Approve / suspend a foster home (status: pending / approved / suspended)
  ✅ Broadcast announcements to the agency's homes
  ✅ Override a placement decision (force ACTIVE, bypassing the parent's accept/deny)
  ✅ Agency report + analytics (Reports tab: placement/incident/goal breakdowns,
       visits, compliance, per-staff performance; staff CSV export)
  ⬜ Process reimbursements / stipends; generate state / federal / outcome reports

CASE WORKER
  ✅ Create a new foster home (a foster parent is REQUIRED — never blank)
  ✅ Assign a child to that foster parent's home, starting on a TRIAL placement
       (default 30 days, or a chosen trial-end date). The child lands as PENDING on
       the foster parent's dashboard — they Accept (→ TRIAL_HOME_VISIT) or Decline,
       and the Y/N is recorded on the placement. After the trial date passes it
       auto-becomes a full ACTIVE placement (placed until reunified), applied
       lazily on view, so it needs no background job.
  ✅ Mark a placement REUNIFIED / ENDED
  ✅ Move a child between the agency's homes (transfer → new PENDING placement,
       old placement ended; the child's placements form a transfer history)
  ✅ Record case-worker visits to a home, OR schedule a future-dated visit
       (SCHEDULED → mark COMPLETED) — date, type, summary
  ✅ Set & track case goals for a placement (OPEN / IN_PROGRESS / MET); the foster
       parent sees them on their dashboard
  ✅ Secure two-way messaging with the foster parent
  ✅ Submit a licensing / compliance item for the foster parent to complete
  ✅ Review / escalate / resolve / close incidents foster parents report
  ✅ View assigned homes & children (case fields only — no medical/private notes)
  ✅ No "Back to app" link (a case worker has no household of their own)

AGENCY VIEWER
  ✅ Read-only oversight: view homes and children
  ✅ Cannot create, assign, or manage anything

Future staff sub-roles (⬜ not yet distinct — would refine CASE_WORKER / VIEWER):
  ⬜ Social Worker        — assessments, care plans, observations, progress, reports
  ⬜ Licensing Specialist — applications, certifications, background checks,
                            inspections, renewals
  ⬜ Intake Coordinator   — intake records, initial documentation, intake status
  ⬜ Supervisor           — review staff work, approve reports, reassign, escalate
  ⬜ Admin Assistant      — calendars, scheduling, forms (no case notes / finance)

PRIVACY (enforced for every role): an agency member can NEVER see another
agency's data, a foster parent's private/medical notes, passwords, or audit
history (the GOLDEN RULE, PART 4).

------------------------------------------------------------------
AGENCY DASHBOARD (Overview tab)
------------------------------------------------------------------
  ✅ Foster homes · children · active placements · staff · compliance alerts
  ✅ Available homes · placement-request (pending accept/deny) queue · open incidents
  ✅ Staff-performance metrics (per-worker placements & visits — Reports tab + CSV)

------------------------------------------------------------------
AGENCY FEATURE CHECKLIST (deduplicated — each item appears once)
------------------------------------------------------------------
Built ✅
  ✅ Multi-agency architecture — each agency isolated by Household.agencyId
  ✅ Separate agency login (/agency-login) + role-aware redirect
  ✅ Staff management (add / remove, role assignment)
  ✅ Foster-home management (create / link / unlink / oversee)
  ✅ Foster-home approval / suspension status (agency oversight)
  ✅ Child placement (assign → foster-parent Accept/Decline (Y/N) → trial→active
       lifecycle → reunify / end), plus transfer between homes + placement history
  ✅ Case-worker visit records (per home)
  ✅ Licensing / compliance submission to a home + downloadable PDF
  ✅ Incident reporting & escalation workflow (foster parent → case worker)
  ✅ Announcements (agency → its homes, shown on the foster-parent dashboard)
  ✅ Court-report PDF generator (per child, from logged data)
  ✅ Case goals (set/track per placement) + secure agency↔foster-parent messaging
  ✅ Scheduled visits (book ahead → mark complete) + training-hours surfaced to agency
  ✅ Placement override + Reports/analytics tab (breakdowns + per-staff CSV)
  ✅ Billing management (Stripe plans; admin Finance / Plans tabs — PART 6)
  ✅ Audit logs (immutable security event on every agency mutation)

Future ⬜ (larger-scope vision — also tracked in PART 4)
  ⬜ Deeper outcome/compliance analytics (trends over time, cohort reporting)
  ⬜ State-reimbursement / payroll / grant tracking
  ⬜ State-compliance reporting · document sharing
  ⬜ Process reimbursements / stipends; state / federal / outcome reports
  ⬜ AI assistant (placement matching, summaries, court reports, risk scoring)
  ⬜ Distinct staff sub-roles (Social Worker / Licensing Specialist / Intake /
       Supervisor / Admin Assistant) — today: Admin / Case Worker / Viewer

------------------------------------------------------------------
FOSTER-PARENT PRODUCT IDEAS (caregiver-facing wishlist — future ⬜)
------------------------------------------------------------------
Kept once, deduped against PART 1 (already built) and the agency list above:
  ✅ Court-report generator (per-child PDF from placements, appointments, active
       meds & recent care-log notes — on the child page)
  ✅ Placement timeline / child journey — merged chronological view per child
       (/dashboard/timeline) from placements, appointments, journal & behaviour
  ✅ "Today" command center (/dashboard/today — appointments, meds, visits,
       court dates, licensing due)
  ✅ Placement-transition packet (per-child PDF: summary, meds, routines, comfort notes)
  ✅ Behavior & trauma tracking (TBRI: triggers, coping, interventions, strengths)
  ✅ Foster closet / inventory (sizes, supplies, restock flag)
  ✅ Family communication hub (/dashboard/communication — call/email/meeting log)
  ✅ Resource library (curated TBRI / IEP / training links)
  ✅ Emergency info mode (/dashboard/emergency — one-tap allergies, meds, contacts)
  ✅ Child story & success journal + keepsake PDF at adoption
  ⬜ Shared team portal (external caseworker/therapist/GAL/teacher scoped access)
  🟡 Mobile app — installable PWA now ships (web app manifest, real PNG/maskable
       icons, service worker, native-style bottom-tab nav). PER-PLATFORM ROADMAP to
       circle back to (install prompts, offline, Web Push, iOS/Android/desktop store
       wrappers, accounts/costs) is documented in MOBILE_PLATFORM_PLAN.md.
  ⬜ Native mobile app (App Store / Play Store wrappers) — see MOBILE_PLATFORM_PLAN.md
       Phases C–E; deferred pending the PWA-only vs store-presence decision.
Already shipped in PART 1 (NOT future): appointments, medications, documents,
care logs, contacts, routines, and licensing-compliance reminders. Foster parents
also: respond (Accept/Decline) to assigned placements, see agency announcements &
caseworker visits, and report incidents — all on the dashboard.
==================================================================
